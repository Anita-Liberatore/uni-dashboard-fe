import { FormatBirthDatePipe } from './format-birth-date.pipe';

describe('FormatBirthDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatBirthDatePipe();
    expect(pipe).toBeTruthy();
  });
});
